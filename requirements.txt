# Core data / geospatial
pandas>=2.2
geopandas>=1.0
shapely>=2.0
pyproj>=3.6
rasterio>=1.3          # hazard-layer raster sampling (dcdata.hazards)
scipy>=1.11            # Poisson intervals, Mann-Whitney (scripts/build_storm_exposure, build_footprint_hazard)
pyogrio>=0.7           # GeoPackage / FileGDB IO used by geopandas

# Schema validation
pydantic>=2.7

# HTTP
requests>=2.32
httpx>=0.27

# Config / secrets
PyYAML>=6.0
python-dotenv>=1.0

# Entity resolution (fuzzy name matching)
rapidfuzz>=3.9

# Poster figures
matplotlib>=3.8
Pillow>=10.0           # Arial glyph metrics for poster box fitting (scripts/build_poster)
python-pptx>=0.6.23
contextily>=1.6        # Figure 1 basemap tiles, fetched once then cached as GeoTIFF

# Dev / quality
pytest>=8.0
ruff>=0.5
